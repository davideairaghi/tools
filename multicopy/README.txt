MultiCopy (mcp) copies a single file to a specified set of destination 
directories (which have to exist) or destination files (which have to be
created if they do not exist, otherwise they will be overwritten).